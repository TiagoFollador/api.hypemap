-- Squad and squad_member are ORM-managed (Prisma schema.prisma).
-- This migration is a no-op placeholder reserved for future raw DDL
-- (e.g., partial indexes, custom constraints, or additional spatial columns on squad tables).
SELECT 1; -- intentional no-op
